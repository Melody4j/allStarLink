# 项目代码架构重构进度 2026-03-13

## 1. 当前总体状态

当前 `src/link_scraper` 的结构化重构已经完成主体工作，项目已进入“基于新结构继续开发”的阶段。

当前进度评估：

- 结构性重构完成度约 `85%`

当前范围说明：

- 当前只关注 `AllStarLink`
- 第二数据源只保留 adapter 骨架
- 当前重点已从“搭架构”转为“数据完整度建模与后续治理”

## 2. 已完成里程碑

### 2.1 Source Adapter 落地

已完成：

- `sources/base.py`
- `sources/allstarlink/client.py`
- `sources/allstarlink/parser.py`
- `sources/allstarlink/mapper.py`
- `sources/factory.py`

结果：

- 主流程不再直接依赖 AllStarLink 原始接口细节

### 2.2 Canonical Model 与 Record Model 落地

已完成：

- `domain/models.py`
- `repositories/records.py`
- `repositories/mappers.py`

结果：

- 统一领域模型与持久化对象分离
- 旧 `Node` 不再是新主流程中心

### 2.3 Service / Repository 分层落地

已完成：

- `services/fetch_service.py`
- `services/parse_service.py`
- `services/sync_service.py`
- `services/ods_service.py`
- `repositories/graph_repository.py`
- `repositories/dim_node_repository.py`
- `repositories/ods_repository.py`

结果：

- 主流程已形成 `worker -> services -> repositories -> managers`

### 2.4 命名治理已完成第一轮

已完成：

- `APIWorker` 主用名切换为 `NodeIngestionWorker`
- `MySQLManager` 主用名切换为 `RelationalStorageManager`
- `Neo4jManager` 主用名切换为 `GraphStorageManager`
- 移除旧命名别名
- 移除 `models/` 旧兼容模型
- 移除 `scrapers/node_parser.py` 包装层

结果：

- 主流程语义更清晰
- 主线代码已统一收敛到新命名和新模型

### 2.5 测试和联调完成

已完成：

- `20` 个测试通过
- `python -m compileall src\link_scraper` 通过
- 一轮真实依赖联调通过

联调已验证：

- Redis 初始化
- MySQL 初始化
- Neo4j 初始化
- 快照扫描
- 节点批量入队
- 单轮节点详情消费

## 3. 联调中修复的问题

本轮联调期间修复：

1. `SnapshotScanner` 批量更新 SQL 变量错误
2. `RelationalStorageManager.execute_query()` 对 DML 返回判断错误

以上问题均已补回归测试。

## 4. 当前未完成项

### 4.1 显式数据完整度建模

未完成：

- 为占位节点和完整节点建立显式状态字段

### 4.2 兼容层彻底清理

已完成：

- 移除旧 `models/`
- 移除 `scrapers/node_parser.py` 包装层
- 移除旧命名别名

### 4.3 第二数据源真实实现

未完成：

- `other_source` 真实 client / parser / mapper

说明：

这是当前明确不推进的范围外事项，不影响当前主线。

## 5. 当前结论

截至 2026-03-13，项目已经具备以下条件：

1. 以 `AllStarLink` 为核心的数据抓取主线可运行
2. 新结构已经具备继续开发条件
3. 文档、代码、测试、联调结论已基本对齐

后续开发应默认以当前重构后的 `src/link_scraper` 结构为准，而不是回退到旧实现思路。
